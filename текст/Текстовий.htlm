 let cart = new Shoppingcart()

 function addToCart(event) {
    let productData = event.target.getAttribute('data-prodect')
    let product JSON.parse(productData)
    cart.addItem(product)
    console.log(cart)
 }




-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
addItem(product){
this.items[product.title] = product

}




------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------




this.saveCartToCookies()

saveCartToCookies() {
    let cart JSON.stringify(tgis.items);
    document.cookie = `cart=${cartJSON}; max-age=${60 * 60 * 24 *
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

}
loadCartFromCookies() {
    tel cartCookie = getCookieValue('cart');
    if (cartCookie && cartCookie !== ''){
        this.items = JSON.parse(cartCookie);
        this.updateCounter();
    }

}
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------